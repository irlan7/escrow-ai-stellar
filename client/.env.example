# URL backend AI recommendation service (lihat folder /server)
# Untuk development lokal, jalankan server di port 3001 (default)
VITE_AI_API_URL=http://localhost:3001

# Untuk production, ganti dengan URL backend yang sudah dideploy (Railway/Render), contoh:
# VITE_AI_API_URL=https://escrow-ai-backend.up.railway.app
